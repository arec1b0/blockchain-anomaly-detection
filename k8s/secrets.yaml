# NOTE: This is a template file.
# Before deploying, replace placeholders with actual base64-encoded values.
# To encode: echo -n "your-secret-value" | base64

apiVersion: v1
kind: Secret
metadata:
  name: redis-secret
  namespace: blockchain-anomaly-detection
  labels:
    app: redis
type: Opaque
data:
  # Base64 encoded Redis password
  # Example: echo -n "your-redis-password" | base64
  password: ""  # REPLACE WITH BASE64 ENCODED PASSWORD (optional)

---
apiVersion: v1
kind: Secret
metadata:
  name: etherscan-secret
  namespace: blockchain-anomaly-detection
  labels:
    app: anomaly-detection
type: Opaque
data:
  # Base64 encoded Etherscan API key
  # Example: echo -n "your-etherscan-api-key" | base64
  api_key: ""  # REPLACE WITH BASE64 ENCODED API KEY

---
apiVersion: v1
kind: Secret
metadata:
  name: sentry-secret
  namespace: blockchain-anomaly-detection
  labels:
    app: anomaly-detection
type: Opaque
data:
  # Base64 encoded Sentry DSN
  # Example: echo -n "https://xxx@sentry.io/xxx" | base64
  dsn: ""  # REPLACE WITH BASE64 ENCODED SENTRY DSN (optional)

---
# Example of how to create secrets via kubectl (alternative to this file)
# kubectl create secret generic redis-secret \
#   --from-literal=password='your-redis-password' \
#   -n blockchain-anomaly-detection
#
# kubectl create secret generic etherscan-secret \
#   --from-literal=api_key='your-etherscan-api-key' \
#   -n blockchain-anomaly-detection
#
# kubectl create secret generic sentry-secret \
#   --from-literal=dsn='your-sentry-dsn' \
#   -n blockchain-anomaly-detection
